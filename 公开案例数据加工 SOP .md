# 公开案例数据加工 SOP

这份文档规定团队如何把公开资料加工成可审核的第一阶段结构化数据。目标不是尽快堆数量，而是保证每条数据都能回答三个问题：原文在哪里、改写了什么、为什么这样标注。

流程到“人工审批通过”为止，不包含模型训练、RAG、知识图谱和银行系统接入。

## 一、最终产物

一条完成的数据由五部分组成：

- 原始来源文件：保存在 `data/raw/public_sources/SRCxxxx/`，保留下载时的原貌；
- 来源记录：保存在 `data/sources/source_registry.jsonl`，记录原文从哪里取得；
- 真实案件记录：保存在 `data/sources/real_case_registry.jsonl`，登记原文中能够独立识别的案件；
- 人工审批稿：保存在 `data/review/public_cases/cases/Cxxx.md`，记录事实边界、改写过程和审批意见；
- 结构化记录：保存在 `data/review/public_cases/structured/Cxxx.json`，供程序读取。

五者通过 `source_id`、`real_case_id` 和 `case_id` 连接：

```text
原始文件及 source_registry.jsonl：SRCxxxx（一份来源）
                         ↓
real_case_registry.jsonl：RCxxxx（原文中的一个真实案件）
                         ↓
cases/Cxxx.md 和 structured/Cxxx.json：Cxxx（一条加工后的数据）
```

一份来源可能记载多个独立真实案件，一个真实案件也可能产生忠实改写、合成变体和正常对照。第三步只保存来源，不拆分、不摘要、不改写；第五步才识别并登记独立真实案件。

## 二、团队执行原则

1. 先保存来源，再开始改写。
2. 来源真实不等于训练文本是原始真实记录。
3. 无法由来源确认的关键情节不能写进“忠实改写”。
4. 事后查明的诈骗结论不能泄漏到事前判断题面。
5. 标签必须由多项事实支持，不能只依赖某个固定风险词。
6. 自动检查只负责发现格式问题，最终结论必须由人审批。
7. 第一阶段只填当前判断真正需要的字段，后续通过版本升级补充。

## 三、完整流程

```text
1. 明确本轮需要什么数据
2. 寻找候选来源
3. 保存并登记来源
4. 核验来源能否使用
5. 拆分并登记独立真实案件
6. 划分事前信息和事后信息
7. 确定数据构造类型
8. 编写事前判断题面
9. 标注答案、类型和证据
10. 编写 Markdown 审批稿
11. 生成 JSON 结构化记录
12. 自动检查和人工审批
```

下面每一步都必须执行。任何一步被退回，都要回到产生问题的步骤修改，不能在后面的 JSON 中掩盖问题。

团队先用一条案例完整走通十二步并交叉审批，再连续处理5条检查一致性；5条稳定后才开始批量加工。

## 四、逐步操作

标注说明：`人工决策` 表示必须由人确定目标；`AI执行` 表示AI或脚本可以完成初稿和机械操作；`人机协作` 表示AI先处理、人再判断。所有正式数据最终仍须人工审批。

### 第1步：明确本轮需要什么数据（人工决策）

**输入：** 当前数据分布、已知模型错误或团队指定的案例主题。

**操作：** 先写清本轮要补的是欺诈案例、正常案例还是信息不足案例，以及要覆盖的主要诈骗类型。不要先抓取大量网页，再反过来寻找用途。

**产物：** 一句明确的收集目标，例如“补充10条虚假投资公开案例，其中至少3条不出现导师、稳赚等明显词”。

**检查：** 目标是否说明数据类别、数量和需要覆盖的差异。

**退回：** 目标只有“再找一些案例”“先做1000条”等数量要求，没有说明要补什么能力。

### 第2步：寻找候选来源（人机协作）

**输入：** 第1步的收集目标。

**操作：** 优先搜索法院、检察院、公安机关、政府部门、金融监管机构和银行发布的案例或风险提示。媒体材料只有在能够追溯到一手来源时才作为正式来源。自媒体内容只能用于发现线索。

来源优先级：

- S级：中央司法机关、国家部委和金融监管机构；
- A级：地方司法机关、公安机关和政府部门；
- B级：能够追溯一手材料的正规媒体；
- C级：自媒体、论坛、无法核验的转载。

第一阶段优先使用S级和A级。B级必须补充一手来源或交叉核验。C级不得直接生成正式数据。

**产物：** 候选来源链接和它可能覆盖的诈骗机制。

**检查：** 页面发布机构是否明确；正文是否包含具体事实；链接是否能够访问。

**退回：** 只有标题或二手概括；页面无法确认发布者；内容只是宣传口号，没有可提取事实。

### 第3步：保存并登记来源（AI执行）

**输入：** 可访问的候选页面。

**操作：** 只做以下四件事：

1. 给整份原文分配一个唯一 `source_id`，例如 `SRC0001`；
2. 建立同名文件夹 `data/raw/public_sources/SRC0001/`；
3. 把下载到的原文保存为 `original.原扩展名`；需要搜索或让程序读取时，再自动生成 `extracted.txt`；
4. 在 `data/sources/source_registry.jsonl` 登记 `source_id` 和原始链接。

第三步不摘要、不核验案件事实、不拆分案件、不改写，也不生成 `real_case_id` 或 `case_id`。

统一目录如下：

```text
data/raw/public_sources/
├── SRC0001/
│   ├── original.html
│   └── extracted.txt（可选）
├── SRC0002/
│   ├── original.pdf
│   └── extracted.txt（可选）
└── SRC0003/
    ├── original.jpg
    └── extracted.txt（可选）
```

保存形式：

- 网页保存为 `original.html`；动态网页无法完整保存时，使用网页PDF或完整截图；
- 官方PDF直接保存为 `original.pdf`；
- 图片材料保存原始图片扩展名，例如 `original.jpg`；
- `extracted.txt` 是可选的程序产物，只用于搜索和AI读取，不要求人工抄写，也不能作为原文证据；
- `original.*` 下载后不得人工修改。需要更正提取错误时，只重新生成 `extracted.txt`。

在 `data/sources/source_registry.jsonl` 只登记：

```json
{
  "source_id": "SRC0001",
  "url": "https://example.com/source"
}
```

`source_id` 是第三步唯一需要创建的编号。原文件路径由固定目录规则直接推出，不重复填写。标题、发布机构、摘要、来源等级和核验结果不在第三步填写。

第三步保存的是“当时获取到的原始资料”，不代表资料中的每项描述已经被核验为真实案件。是否属于可用来源、原文包含几个独立真实案件，分别在第四步和第五步判断。

同一网页只登记一个稳定的 `source_id`。

**产物：** 一个 `source_id` 文件夹、一个原始文件和一条 `source_id + url` 来源记录；`extracted.txt` 按需生成。

**检查：** `source_id` 是否唯一；原文件能否打开；文件夹名和登记的 `source_id` 是否一致；原始链接是否正确。

**退回：** 只保存链接而没有原文件；只复制正文到TXT却没有保存原始HTML、PDF或图片；`source_id` 重复；原文件被人工修改。

### 第4步：核验来源能否使用（人机协作）

**输入：** 来源记录和正文内容。

**操作：** 判断资料属于以下哪一种：

- 具体案件：描述了能够区分的当事行为、过程或处置结果；
- 典型机制：说明一种常见套路，但没有独立案件事实；
- 风险提示：只给防范建议或概括性警告。

三类资料都可以保留，但用途不同。具体案件可以作为公开案例改写的基础；典型机制适合受控合成；风险提示主要用于核对规则，不能冒充独立真实案件。

**产物：** 来源用途结论和核验理由。

**检查：** 是否明确区分“发生过的具体案件”和“官方总结的诈骗类型”。

**退回：** 把一篇“十大诈骗类型”文章直接当成10个真实独立案件；仅因为来自官方网站就认为其中每个例子都是生效判决。

### 第5步：拆分并登记独立真实案件（人机协作）

**输入：** 已核验的来源正文。

**操作：** 依据事件主体、行为链和案件结果判断能否拆分。只有来源明确描述多个彼此独立事件时，才建立多个 `real_case_id`。同一犯罪团伙针对多人实施同一种行为，不自动等于多个独立真实案件。

每个真实案件在 `data/sources/real_case_registry.jsonl` 登记一次：

```json
{
  "real_case_id": "RC0001",
  "source_id": "SRC0002",
  "source_locator": "案例一：余某等诈骗案",
  "fact_summary": "通过社交平台建立联系，展示投资盈利内容并引导进入指定平台投入资金。"
}
```

`source_locator` 用于定位原文中的标题、段落或页码；`fact_summary` 只能概括原文明确支持的事实，不能加入改编情节。此时仍然不编写训练题面。

**产物：** 每个独立真实案件的 `real_case_id`，格式为 `RC` 加四位数字，例如 `RC0001`。

**检查：** 每个 `real_case_id` 是否能通过 `source_locator` 定位到原文；是否保留正确的 `source_id`；摘要是否忠于原文。

**退回：** 为增加数量，把同一段事实换几种说法后分配多个 `real_case_id`；无法指出案件在原文中的位置；把官方总结的诈骗机制登记成已经发生的独立真实案件。

### 第6步：划分事前信息和事后信息（人机协作）

**输入：** 已登记的真实案件、典型机制或正常场景。

**操作：** 把事实分成三组：

- 事前可见信息：用户在付款或操作前可能知道并能描述的接触方式、对方身份主张、承诺、核验情况和请求动作；
- 事后确认信息：警方或法院后来确认的团伙身份、后台控制、最终损失、完整资金流、抓捕和判决结果；
- 原文未提供信息：金额、账户、设备、IP或历史交易等来源没有说明的内容。

只有事前可见信息可以直接进入 `text`。事后信息只用于核对答案；原文未提供的信息不得冒充原案事实。

**产物：** 事前可见、事后确认和原文未提供三类事实清单。

**检查：** 如果遮住标签，只看 `text`，它是否像用户在决定操作前可能提出的问题。

**退回：** `text` 中出现“警方认定”“诈骗团伙控制”“最终被骗”“法院判决”等直接答案；银行未接入的信息被写成系统已经掌握。

第6步是流程分界：此前处理来源和原始事实，此后才开始构造训练数据。

### 第7步：确定数据构造类型（人机协作）

**输入：** 第6步形成的事实清单，以及本条数据计划保留、删除或新增的内容。

**操作：** 从以下三类中选择一个 `construction_type`：

- `public_case_rewritten`：关键事实都能由公开来源支持，只做脱敏、压缩和事前场景化；
- `controlled_synthetic`：基于真实机制，但新增或改变了来源无法确认的关键情节；
- `normal_counterfactual`：围绕相似场景构造正常对照，防止模型只凭主题词判断欺诈。

原案是现金交付，题面却改成银行转账，应标为 `controlled_synthetic`，不能标为忠实改写。

判断时分别核对三件事：底层来源是否可靠、题面是否忠于来源、实际采用了哪种构造方式。来源真实不代表题面中的新增情节也是真实事实。

**产物：** 数据构造类型和选择理由。

**检查：** 计划写入的每个关键动作是否有原文支持；新增事实是否如实标为合成。

**退回：** 补造收款账户、支付渠道、设备、IP、历史交易或银行风险名单，却仍标记为 `public_case_rewritten`。

### 第8步：编写事前判断题面（AI执行）

**输入：** 允许进入题面的事实清单。

**操作：** 用第一人称或清晰的场景描述编写 `text`。题面要保留判断所需事实，但不得直接告诉模型答案。

题面至少说明：如何接触、对方如何建立可信度、对方希望用户做什么。来源没有说明的内容保持未知，不为追求完整而补造。

不使用过度具体的词作为唯一信号。例如，不依赖“指导老师”“伦敦金”本身，而保留更通用的行为关系：陌生身份主张、单方展示收益、引导进入指定平台、要求资金动作、无法独立核验。

**产物：** 一段可以直接输入模型的 `text`。

**检查：** 题面是否自然；是否包含足够事实；是否存在答案词；是否混入事后信息；是否虚构银行可见数据。

**退回：** 仅由“这是一个诈骗案例”改写而来；删除全部关键事实导致无法判断；通过加入“骗局”“虚假平台”等词强行使答案明显。

### 第9步：标注答案、类型和证据（AI执行）

**输入：** 最终 `text` 和经过核验的来源事实。

**操作：** 填写 `label`、`fraud_type` 和 `evidence`。

`label` 只允许：

- `fraud`：当前事实足以支持欺诈判断；
- `normal`：当前行为有合理、可核验的正常解释；
- `insufficient`：现有信息不足以在欺诈和正常之间作出可靠判断。

`fraud_type` 使用团队统一枚举：

- `fake_investment`
- `task_rebate`
- `impersonated_customer_service`
- `impersonated_law_enforcement`
- `impersonated_acquaintance_or_leader`
- `loan_or_credit`
- `phishing_or_account_takeover`
- `ticket_game_virtual_goods`
- `romance_or_trust_fraud`
- `ai_impersonation`
- `other_fraud`
- `normal`
- `insufficient`

`label=normal` 时，`fraud_type` 必须是 `normal`；`label=insufficient` 时必须是 `insufficient`。遇到无法归类的欺诈先使用 `other_fraud`，不得由个人临时创造新类别。

`evidence` 只记录 `text` 中可以直接定位的决定性短语。第一阶段要求尽量使用题面原句，至少一项；不能把来源中的事后结论塞进证据。

**产物：** 一组相互一致的监督答案。

**检查：** 标签是否由多项事实共同支持；每条证据能否在题面中找到；类型是否符合枚举和标签一致性规则。

**退回：** 只凭“导师”“客服”“投资”等单词判为欺诈；证据来自法院判决但没有出现在题面；正常案例的类型仍写成某类欺诈。

### 第10步：编写 Markdown 审批稿（AI执行）

**输入：** 来源记录、事实边界、题面和标注结果。

**操作：** 在 `cases/Cxxx.md` 中写清以下内容：

```markdown
# Cxxx｜案例名称

## 来源
- source_id：
- real_case_id：
- 标题：
- 发布机构：
- 原始链接：
- 发布日期：
- 访问日期：
- 来源等级：
- 必要原文摘录：

## 数据性质
- construction_type：
- 选择理由：

## 事实边界
- 可进入题面的事前事实：
- 只能用于核对答案的事后事实：
- 来源没有提供的信息：

## 结构化草稿
- text：
- label：
- fraud_type：
- evidence：

## 人工审批
- review_status：pending
- reviewer：
- reviewed_at：
- review_note：
```

必要原文摘录只保留支持当前案例的最小范围，并遵守版权、隐私和脱敏要求。

**产物：** 一份人能够核对全部关键判断的审批稿。

**检查：** 审批人是否无需重新搜索网页，就能看出题面事实来自哪里、删除了什么、增加了什么。

**退回：** 只有评分没有事实依据；只有改写文本没有原文摘录；结构化 JSON 中出现审批稿没有记录的新事实。

### 第11步：生成 JSON 结构化记录（AI执行）

**输入：** 已完成的 Markdown 审批稿。

**本阶段目标：** 第一阶段只训练和评测“根据当前题面判断是否欺诈”。JSON 不负责追问、风险分级和干预决策。

#### 11.1 为什么要生成 JSON

Markdown 用于人工阅读和审批，JSON 用于程序校验、训练和评测。第11步不再创造事实或修改答案，只把第10步已经确认的内容转换成固定格式。

每个 JSON 只表达一个判断场景：一个时间点、一段题面、一个主标签和一组证据。即使来自同一个真实案件，不同时间点或不同信息量的题面也必须使用不同的 `case_id`。

#### 11.2 第一阶段字段

在 `structured/Cxxx.json` 生成第一阶段记录，固定使用以下十个字段：

```json
{
  "case_id": "C051",
  "real_case_id": "RC0001",
  "text": "我在社交平台认识了一名自称投资导师的人。对方发来群内盈利截图，并让我在一个尚未独立核验的平台充值。",
  "label": "fraud",
  "fraud_type": "fake_investment",
  "evidence": [
    "自称投资导师",
    "发来群内盈利截图",
    "让我在一个尚未独立核验的平台充值"
  ],
  "source_id": "SRC0002",
  "construction_type": "public_case_rewritten",
  "review_status": "pending",
  "schema_version": "1.0"
}
```

十个字段分成四组：

**追溯字段：**

- `case_id`：结构化案例唯一编号；
- `real_case_id`：这条数据依据的独立真实案件编号；`public_case_rewritten` 必须填写。没有具体真实案件依据的 `controlled_synthetic` 或 `normal_counterfactual` 可以填 `null`；
- `source_id`：来源登记表中的稳定编号；
- `construction_type`：忠实改写、受控合成或正常对照。

**模型输入字段：**

- `text`：模型在判断时唯一允许看到的题面；

**参考答案字段：**

- `label`：欺诈、正常或信息不足；
- `fraud_type`：统一的主类型；
- `evidence`：题面中支持答案的决定性事实。

**治理字段：**

- `review_status`：人工审批状态；
- `schema_version`：当前结构版本，第一阶段固定为 `1.0`。

`review_status` 只允许 `draft`、`pending`、`approved`、`disputed` 和 `rejected`。生成 JSON 后使用 `pending`；只有审批人可以改成 `approved`。

#### 11.3 颗粒度标准

- `text`：只描述一次判断所需的事前信息，不混入案件经过、参考答案、追问或处置建议；
- `label`：每条数据只能有一个主标签，取值为 `fraud`、`normal` 或 `insufficient`；
- `fraud_type`：只保留一个主类型。第一阶段不增加次级类型和多标签；
- `evidence`：通常保留2至5条决定性短语，必须能在 `text` 中直接定位。欺诈案例记录风险行为组合，正常案例记录可核验的正常闭环，信息不足案例记录造成歧义的已知事实；
- `case_id`：不同题面使用不同编号，不能只修改原 JSON 覆盖另一种题面；
- 缺失信息保持缺失，不能用空字符串、猜测值或默认值伪装成已知事实。

不要加入 `confidence`、`risk_level`、`missing_information`、`recommended_questions`、`recommended_intervention`、`conversation`、`difficulty`、`split` 等字段。它们不服务第一阶段的欺诈判断，会增加审批成本并混淆训练目标。

#### 11.4 生成顺序

1. 从审批稿复制 `case_id`、`source_id`、`real_case_id` 和 `construction_type`；
2. 从第8步复制最终 `text`；
3. 从第9步复制 `label`、`fraud_type` 和 `evidence`；
4. 将 `review_status` 设为 `pending`，将 `schema_version` 设为 `1.0`；
5. 按第12步自动检查，不得在生成过程中补写审批稿中不存在的内容。

#### 11.5 后续阶段怎么扩展

当前只实施第一阶段。以后确实开始训练其他能力时再升级 Schema：

- 第二阶段训练追问能力，再增加缺失信息和建议问题；
- 第三阶段训练处置能力，再增加风险等级和干预建议。

后续扩展时保留现有字段及其含义，通过新的 `schema_version` 增加字段并编写迁移脚本。一个案例只保留一份权威结构化记录，不长期人工维护两套内容重复但字段不同的 JSON。

**产物：** 可以被程序解析的单案例 JSON 文件。

**检查：** 十个字段是否齐全且没有阶段外字段；字段值是否与 Markdown 完全一致；`evidence` 是否能在 `text` 中定位；`source_id` 和 `real_case_id` 是否存在并相互对应；JSON 是否可解析。

**退回：** JSON 混入追问、干预等后续字段；一条记录包含多个判断时间点；自动生成程序带入空字符串、猜测内容或旧字段；JSON 与审批稿不同；自动检查通过后直接写成 `approved`。

### 第12步：自动检查和人工审批（人机协作，人工终审）

**输入：** Markdown 审批稿和 JSON 文件。

**操作：** 先自动检查，再人工审批。

自动检查至少包括：

- JSON 语法正确；
- 十个必填字段完整；
- `case_id` 唯一且文件名一致；
- `source_id` 能在来源登记表中找到；
- 非空的 `real_case_id` 能在真实案件登记表中找到，并且对应同一个 `source_id`；`public_case_rewritten` 不允许为空；
- 枚举值合法，标签与类型一致；
- `evidence` 非空，并能在 `text` 中直接定位；
- 文本不存在明显未脱敏的手机号、银行卡号、身份证号等信息；
- 重复文本和高度相似文本被标出供人工复核。

人工审批必须检查：

- 来源是否真的支持题面关键事实；
- 构造类型是否诚实；
- 是否存在事后答案泄漏；
- 标签和类型是否合理；
- 证据是否足以支持答案；
- 是否只靠固定关键词判断；
- 是否虚构银行可以获取的信息。

审批结论：

- `approved`：来源、题面、标签和证据均通过；
- `disputed`：存在需要团队共同判断的问题，暂不使用；
- `rejected`：存在明确错误，退回修改或废弃。

只有自动检查通过、人工状态为 `approved`、审批记录已写回 Markdown 且修改已由 Git 留痕，才算正式完成；其他状态都只能算草稿。

**产物：** 有明确人工状态的正式数据或退回记录。

**检查：** 审批人、时间和理由是否写回 Markdown；JSON 状态是否与审批稿一致。

**退回：** 只有AI评分或自动脚本结论，没有人工审批；审批理由为空；修改 JSON 后没有重新检查。
