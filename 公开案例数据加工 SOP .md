# 公开案例数据加工 SOP

这份文档规定团队如何把公开资料加工成可审核的第一阶段结构化数据。目标不是尽快堆数量，而是保证每条数据都能回答三个问题：原文在哪里、改写了什么、为什么这样标注。

流程到“人工审批通过”为止，不包含模型训练、RAG、知识图谱和银行系统接入。

## 一、最终产物

一条完成的数据由五部分组成：

- 原始来源文件：保存在 `data/raw/public_sources/SRCxxxx/`，保留下载时的原貌；
- 来源记录：保存在 `data/sources/source_registry.jsonl`，记录原文从哪里取得；
- 真实案件记录：保存在 `data/sources/real_case_registry.jsonl`，登记原文中能够独立识别的案件；
- 人工审批稿：保存在 `data/review/public_cases/cases/Cxxxx-Tnn.md`，记录事实边界、改写过程和审批意见；
- 结构化记录：保存在 `data/review/public_cases/structured/Cxxxx-Tnn.json`，供程序读取。

五者通过 `source_id`、`real_case_id` 和 `case_id` 连接：

```text
原始文件及 source_registry.jsonl：SRCxxxx（一份来源）
                         ↓
real_case_registry.jsonl：RCxxxx（原文中的一个真实案件）
                         ↓
cases/Cxxxx-Tnn.md 和 structured/Cxxxx-Tnn.json：Cxxxx-Tnn（一条加工后的数据）
```

一份来源可能记载多个独立真实案件，一个真实案件也可能产生忠实改写、合成变体和正常对照。第三步只保存来源，不拆分、不摘要、不改写；第五步才识别并登记独立真实案件。

### 案例命名原则

`case_id` 固定使用 `Cxxxx-Tnn`，由三部分组成：

- `Cxxxx`：案例组编号。四位数字必须与母案例 `RCxxxx` 一致，例如 `RC0001` 只能生成 `C0001-*`；
- `T`：构造类型。`F` 表示忠实改写，`E` 表示增强欺诈案例，`N` 表示正常对照；
- `nn`：同一案例组、同一类型内的两位序号，从 `01` 开始递增。例如第二条增强案例使用 `E02`。

因此，`RC0001` 的第一条忠实改写、增强案例和正常对照分别命名为 `C0001-F01`、`C0001-E01` 和 `C0001-N01`。

执行规则：

1. 文件名、Markdown 标题、Markdown 中的 `case_id` 和 JSON 中的 `case_id` 必须完全一致；
2. 不再使用 `C001`、`C007` 这类无法直接看出案例组和构造类型的全局流水号；
3. 某类变体不生成时不创建占位编号，其他类型仍从 `01` 开始；
4. 同一题面在审批前修改可以沿用原编号；审批通过后如形成新的判断场景，必须递增该类型序号，不能覆盖旧案例；
5. 已经进入版本记录的编号不得转给其他案例重复使用。

## 二、团队执行原则

1. 先保存来源，再开始改写。
2. 来源真实不等于训练文本是原始真实记录。
3. 无法由来源确认的关键情节不能写进“忠实改写”。
4. 事后查明的诈骗结论不能泄漏到事前判断题面。
5. 标签必须由多项事实支持，不能只依赖某个固定风险词。
6. 自动检查只负责发现格式问题，最终结论必须由人审批。
7. 第一阶段只填当前判断真正需要的字段，后续通过版本升级补充。

## 三、完整流程

```text
1. 明确本轮需要什么数据
2. 寻找候选来源
3. 保存并登记来源
4. 核验来源能否使用
5. 拆分并登记独立真实案件
6. 划分事前信息和事后信息
7. 编写事前判断题面并确定构造类型
8. 标注答案、类型和证据
9. 完善 Markdown 审批稿
10. 生成 JSON 结构化记录
11. 自动检查和人工审批
```

下面每一步都必须执行。任何一步被退回，都要回到产生问题的步骤修改，不能在后面的 JSON 中掩盖问题。

团队先用一条案例完整走通十一步并交叉审批，再连续处理5条检查一致性；5条稳定后才开始批量加工。

## 四、逐步操作

标注说明：`人工决策` 表示必须由人确定目标；`AI执行` 表示AI或脚本可以完成初稿和机械操作；`人机协作` 表示AI先处理、人再判断。所有正式数据最终仍须人工审批。

### 第1步：明确本轮需要什么数据（人工决策）

**输入：** 当前数据分布、已知模型错误或团队指定的案例主题。

**操作：** AI先根据当前数据数量、类型分布和已知模型错误，起草本轮收集目标。目标必须写明数据类别、数量、主要诈骗类型和希望补足的识别能力，具体数量根据当前任务决定，不在SOP中固定。人只负责确认目标，或直接修改目标中的错误，不需要从空白开始撰写。

在目标确认前不得批量搜索或下载来源。第一轮只处理1条案例，完整跑通流程后再连续处理5条。

**产物：** 一句明确的收集目标，例如“补充10条虚假投资公开案例，其中至少3条不出现导师、稳赚等明显词”。

**检查：** 目标是否说明数据类别、数量和需要覆盖的差异。

**退回：** 目标只有“再找一些案例”“先做1000条”等数量要求，没有说明要补什么能力。

### 第2步：寻找候选来源（人机协作）

**输入：** 第1步的收集目标。

**操作：** AI根据第1步的目标搜索候选来源，优先使用法院、检察院、公安机关、政府部门、金融监管机构和银行发布的材料。媒体材料只有在能够追溯到一手来源时才作为正式来源；自媒体内容只能用于发现线索。

AI根据当前任务提交适量候选来源，并为每个候选统一给出：发布者、原始链接、预计来源等级、可能属于的材料类型、可能覆盖的诈骗机制和主要限制。存在多个候选时，AI必须明确推荐一个。人只负责选择候选来源，或指出候选不符合目标，不需要重新整理候选信息。

来源优先级：

- S级：中央司法机关、国家部委和金融监管机构；
- A级：地方司法机关、公安机关和政府部门；
- B级：能够追溯一手材料的正规媒体；
- C级：自媒体、论坛、无法核验的转载。

第一阶段优先使用S级和A级。B级必须补充一手来源或交叉核验。C级不得直接生成正式数据。

**产物：** 格式一致的候选来源；存在多个候选时包含一个明确的推荐项。

**检查：** 页面发布机构是否明确；正文是否包含具体事实；链接是否能够访问。

**退回：** 只有标题或二手概括；页面无法确认发布者；内容只是宣传口号，没有可提取事实。

### 第3步：保存并登记来源（AI执行）

**输入：** 可访问的候选页面。

**操作：** 只做以下四件事：

1. 给整份原文分配一个唯一 `source_id`，例如 `SRC0001`；
2. 建立同名文件夹 `data/raw/public_sources/SRC0001/`；
3. 把下载到的原文保存为 `original.原扩展名`；需要搜索或让程序读取时，再自动生成 `extracted.txt`；
4. 由AI或脚本在 `data/sources/source_registry.jsonl` 创建固定格式的来源草稿。

第三步不摘要、不核验案件事实、不拆分案件、不改写，也不生成 `real_case_id` 或 `case_id`。

统一目录如下：

```text
data/raw/public_sources/
├── SRC0001/
│   ├── original.html
│   └── extracted.txt（可选）
├── SRC0002/
│   ├── original.pdf
│   └── extracted.txt（可选）
└── SRC0003/
    ├── original.jpg
    └── extracted.txt（可选）
```

保存形式：

- 网页保存为 `original.html`；动态网页无法完整保存时，使用网页PDF或完整截图；
- 官方PDF直接保存为 `original.pdf`；
- 图片材料保存原始图片扩展名，例如 `original.jpg`；
- `extracted.txt` 是可选的程序产物，只用于搜索和AI读取，不要求人工抄写，也不能作为原文证据；
- `original.*` 下载后不得人工修改。需要更正提取错误时，只重新生成 `extracted.txt`。

`source_registry.jsonl` 每行保存一个来源对象。第三步必须由AI或脚本生成，禁止人工从空白逐字段录入。固定格式如下：

```json
{
  "source_id": "SRC0001",
  "url": "https://example.com/source",
  "title": null,
  "published_at": null,
  "accessed_at": "2026-08-16",
  "publisher": null,
  "content_source": null,
  "source_level": null,
  "material_types": [],
  "is_usable": null,
  "usable_scope": [],
  "limitations": [],
  "verification_reason": null,
  "verification_status": "pending"
}
```

第三步只填写 `source_id`、`url` 和实际保存原文当天的 `accessed_at`，其余字段按固定默认值生成。`source_id` 由程序读取现有最大编号后递增产生，不能由人手工猜测。原文件路径由固定目录规则直接推出，不重复填写。第三步不填写发布机构、来源等级和核验结论。

字段格式在第三步即被锁定，后续只能更新字段值，不能由个人临时增加或改名：

- `source_level` 只允许 `S`、`A`、`B`、`C` 或 `null`；
- `published_at` 使用页面标注的发布日期，`accessed_at` 使用实际保存原文的日期，格式均为 `YYYY-MM-DD`；无法确认发布日期时 `published_at` 使用 `null`，`accessed_at` 不得为空；
- `material_types` 只允许包含 `specific_case`、`typical_mechanism`、`risk_warning`、`normal_process`；
- `is_usable` 只允许 `true`、`false` 或 `null`；
- `usable_scope` 和 `limitations` 必须是字符串数组；
- `verification_status` 只允许 `pending` 或 `approved`；
- 尚未核验的单值使用 `null`，尚未填写的多值列表使用 `[]`，不得使用空字符串代替未知值。

第三步保存的是“当时获取到的原始资料”，不代表资料中的每项描述已经被核验为真实案件。是否属于可用来源、原文包含几个独立真实案件，分别在第四步和第五步判断。

同一网页只登记一个稳定的 `source_id`。

**产物：** 一个 `source_id` 文件夹、一个原始文件和一条字段完整但核验状态为 `pending` 的来源草稿；`extracted.txt` 按需生成。

**检查：** `source_id` 是否唯一且连续生成；原文件能否打开；文件夹名和登记的 `source_id` 是否一致；原始链接是否正确；固定字段是否齐全；默认值是否正确。

**退回：** 只保存链接而没有原文件；只复制正文到TXT却没有保存原始HTML、PDF或图片；`source_id` 重复；原文件被人工修改；人工手写造成字段缺失、字段改名或使用空字符串表示未知值。

### 第4步：核验来源能否使用（人机协作）

**输入：** 来源记录和正文内容。

**操作：** AI读取原始文件并生成核验草稿，人只核对和修改草稿。AI必须从以下四个角度判断：

1. 谁发布：确认网页实际发布者、内容标注来源和来源等级；
2. 材料性质：判断资料包含具体案件、典型机制、风险提示或正常流程，可多选；
3. 事实充分性：判断是否存在可以定位的主体、接触方式、信任建立、请求动作和结果；
4. 使用边界：写清哪些部分可以继续加工，以及哪些事实不能从该来源推出。

材料类型定义如下：

- 具体案件：描述了能够区分的当事行为、过程或处置结果；
- 典型机制：说明一种常见套路，但没有独立案件事实；
- 风险提示：只给防范建议或概括性警告；
- 正常流程：由官方或业务主体公开的正常办理流程，具有可核验的渠道、办理主体和完成路径。

四类资料都可以保留，但用途不同。具体案件可以作为公开案例改写的基础；典型机制适合受控合成；风险提示主要用于核对规则，不能冒充独立真实案件；正常流程用于构造和核验正常类数据，不能被改写成真实诈骗案件。

AI在 `source_registry.jsonl` 的原记录中填写 `title`、`published_at`、`publisher`、`content_source`、`source_level`、`material_types`、`is_usable`、`usable_scope`、`limitations` 和 `verification_reason`，并保持 `verification_status` 为 `pending`。

人不需要重新撰写核验结论，也不需要写“通过”或“驳回”的说明。人只做两件事：修改AI草稿中的错误；确认无误后，把 `verification_status` 从 `pending` 改成 `approved`。

`verification_status=approved` 表示人工确认了核验结论，不表示来源一定可用。来源是否可用由 `is_usable` 单独表达：

```json
{
  "is_usable": false,
  "verification_status": "approved"
}
```

以上状态表示“人工确认该来源不可用于后续案例加工”。只有 `is_usable=true` 且 `verification_status=approved` 的来源可以进入第5步。

**产物：** 一条由AI生成、人工修改并标记为 `approved` 的完整来源记录。

**检查：** 是否明确区分“发生过的具体案件”和“官方总结的诈骗类型”；字段值是否符合第3步枚举；可用范围能否在原文中定位；人工是否只在完成核对后标记 `approved`。

**退回：** 把一篇“十大诈骗类型”文章直接当成10个真实独立案件；仅因为来自官方网站就认为其中每个例子都是生效判决；AI直接把自己的草稿写成 `approved`；`is_usable` 与核验理由明显矛盾。

### 第5步：拆分并登记独立真实案件（人机协作）

**输入：** 已核验的来源正文。

**操作：** 依据事件主体、行为链和案件结果判断能否拆分。只有来源明确描述多个彼此独立事件时，才建立多个 `real_case_id`。同一犯罪团伙针对多人实施同一种行为，不自动等于多个独立真实案件。

每个真实案件在 `data/sources/real_case_registry.jsonl` 登记一次：

```json
{
  "real_case_id": "RC0001",
  "source_id": "SRC0002",
  "source_locator": "案例一：余某等诈骗案",
  "fact_summary": "通过社交平台建立联系，展示投资盈利内容并引导进入指定平台投入资金。",
  "verification_status": "pending"
}
```

`source_locator` 用于定位原文中的标题、段落或页码；`fact_summary` 只能概括原文明确支持的事实，不能加入改编情节。此时仍然不编写训练题面。

AI生成真实案件草稿时，`verification_status` 必须为 `pending`。人只核对案件拆分、原文位置和事实摘要；确认无误后，将其改为 `approved`。只有 `verification_status=approved` 的真实案件可以进入第6步。

**产物：** 每个独立真实案件的 `real_case_id`，格式为 `RC` 加四位数字，例如 `RC0001`。

**检查：** 每个 `real_case_id` 是否能通过 `source_locator` 定位到原文；是否保留正确的 `source_id`；摘要是否忠于原文；人工核对后是否标记为 `approved`。

**退回：** 为增加数量，把同一段事实换几种说法后分配多个 `real_case_id`；无法指出案件在原文中的位置；把官方总结的诈骗机制登记成已经发生的独立真实案件；AI直接把未经人工核对的草稿标为 `approved`。

### 第6步：划分事前信息和事后信息（人机协作）

**输入：** 已登记的真实案件、典型机制或正常场景。

**操作：** 先确定 `decision_point`，再把事实分组。`decision_point` 是智能体作出判断的时间截点；本项目默认设在“本次资金操作执行前”。如果原文没有给出某一笔交易的时间和金额，只能使用原文能够支持的行为节点，例如“小额试验完成后、准备继续投入资金前”，不能虚构具体笔次。

所有事实必须相对于这个判断时点划分：

- 事前可见信息：在判断时点之前已经发生，并且用户当时可能知道和描述的接触方式、对方身份主张、承诺、核验情况和请求动作；
- 事后确认信息：在判断时点之后才发生，或者只能通过完整案件回溯、警方或法院调查确认的累计投入、无法提现、团伙身份、后台控制、最终损失、完整资金流、抓捕和判决结果；
- 原文未提供信息：金额、账户、设备、IP或历史交易等来源没有说明的内容。

只有事前可见信息可以直接进入 `text`。事后信息只用于核对答案；原文未提供的信息不得冒充原案事实。

AI在对应的 `real_case_registry.jsonl` 记录中生成以下草稿：

```json
{
  "decision_point": "本次资金操作执行前",
  "pre_event_facts": [],
  "post_event_facts": [],
  "missing_information": [],
  "boundary_verification_status": "pending"
}
```

- `decision_point`：本条数据划分事前和事后的唯一时间截点；
- `pre_event_facts`：判断时点前已经发生，且当事人当时可能知道并描述的信息；
- `post_event_facts`：判断时点后发生，或只能通过完整案件回溯确认的信息；
- `missing_information`：原文没有提供、但后续判断容易被误补的信息，只记录与当前任务有关的缺口，不穷举所有未知事实。

最终累计投入、最终损失等案件回溯数字，即使发生在受害人发现被骗之前，只要在所选判断时点尚未完整发生，就不能放入 `pre_event_facts`。来源没有给出逐笔交易时序时，也不能把最终累计金额拆成某一笔事前可见金额。

AI负责从原文生成三组事实草稿，不能自行将 `boundary_verification_status` 改为 `approved`。人只核对分类是否正确、是否遗漏关键事实、是否加入原文不存在的内容；确认无误后，将状态从 `pending` 改为 `approved`。第5步的 `verification_status` 与第6步的 `boundary_verification_status` 分别记录案件登记审批和信息边界审批，不能互相替代。

**产物：** 三类事实清单，以及等待人工审批的 `boundary_verification_status`。

**检查：** `decision_point` 是否明确且由原文时序支持；每项事实在该时点是否已经发生并可被当事人知道；最终累计金额和事后结果是否混入事前信息；原文未提供信息是否只记录与当前任务有关的缺口；人工核对后是否标记为 `approved`。

**退回：** 把“无法提现”“被拉黑”“发现被骗”等结果放入事前信息；把对方真实身份、后台控制、资金流向或银行未接入的信息写成已知事实；AI直接把草稿标为 `approved`。

只有 `boundary_verification_status=approved` 的记录可以进入第7步。

第6步是流程分界：此前处理来源和原始事实，此后才开始构造训练数据。

### 第7步：编写事前判断题面并确定构造类型（人机协作）

**输入：** 第6步已经审批的 `decision_point`、`pre_event_facts`、`post_event_facts` 和 `missing_information`。

**操作：** 第7步开始进入加工数据层。AI先按案例组和构造类型分配唯一 `case_id`，创建 `data/review/public_cases/cases/Cxxxx-Tnn.md`，再按以下顺序生成草稿：

1. 只使用允许进入题面的事前事实编写 `text`；
2. 把 `text` 中每个关键事实与来源和事实边界逐项对照；
3. 根据实际写成的题面确定 `construction_type`，并说明选择理由；
4. 将 `content_verification_status` 设为 `pending`，等待人工同时审批题面和构造类型。

题面使用第一人称或清晰的场景描述，至少说明如何接触、对方如何建立可信度、对方希望用户做什么。来源没有说明的内容保持未知，不为追求完整而补造；不能出现“骗局”“诈骗团伙”“警方认定”等直接答案，也不能混入判断时点之后的信息。

不把“指导老师”“伦敦金”等具体词作为唯一信号，而保留更通用的行为关系，例如陌生身份主张、单方展示收益、引导进入指定平台、要求资金动作和缺少独立核验。

`construction_type` 只允许：

- `public_case_rewritten`：题面关键事实都能由公开来源支持，只做脱敏、压缩和事前场景化；
- `controlled_synthetic`：基于真实机制生成的受控变体。题面新增或改变了来源无法确认的关键情节，用于测试模型能否识别更隐蔽的行为关系；
- `normal_counterfactual`：围绕相似场景构造正常对照，通过独立核验、正规渠道和正常资金闭环，防止模型只凭主题词判断欺诈。

构造类型必须根据最终 `text` 判定，而不是根据写作计划预先判定。例如原案是现金交付，题面却写成银行转账，就必须标为 `controlled_synthetic`。

`construction_type` 只记录题面如何产生，不预设 `label`；第8步必须只根据最终 `text` 标注答案。省略金额、日期或具体称谓等非决定性信息时，标签可以不变；省略核验方式、收款对象、资金用途或具体操作等决定性信息后无法可靠判断时，必须标为 `insufficient`。简略、口语化变体按需生成，沿用原构造类型并递增该类型序号，不新增构造类型或字段。

具体改写方法：

1. 忠实改写 `public_case_rewritten`：从已经审批的事前事实中选择一个判断时点，删除事后结果，压缩成第一人称或清晰场景。只能删减、脱敏和调整表达，不能增加关键事实；
2. 受控变体 `controlled_synthetic`：先复制忠实改写，再选择身份包装、接触渠道、资金方式或核验关系中的一个维度进行改变。可以增加合理的可信包装，但题面中必须保留能够被发现的关系异常；
3. 正常对照 `normal_counterfactual`：保留原案例的主题和部分高混淆表面特征，把决定性风险关系替换为独立核验、主体一致和正常资金闭环。不能只删除风险词，也不能把客观危险行为解释成正常。

“选择理由”统一说明四件事：保留了什么、改变了什么、新增了什么、这条数据测试什么能力。忠实改写没有新增事实时必须明确写明“未新增关键事实”。

案例组规则：

- 同一 `real_case_id` 下的所有 `case_id` 属于同一案例组；
- 每个可用真实案件至少生成一条 `public_case_rewritten`；
- `public_case_rewritten` 保留原案已经审批的事前事实，不补造关键情节；
- `controlled_synthetic` 只在存在明确训练目标和合理改变维度时生成。每条只重点改变身份包装、接触渠道或资金请求方式等一个维度，不能加入“尚未核验”“看似正规”等直接提示答案的作者判断；
- `normal_counterfactual` 只在能够形成可信、可独立核验的正常闭环时生成。正常结论必须由题面中的正面核验事实支持，不能只罗列“不转账”“不共享”等安全词，也不能用合成正常对照替代独立收集的真实正常案例；
- 无法合理生成某类变体时不生成、不占号，不得为了凑齐三条而编造；欺诈、增强和正常数据的数量在整个批次层面检查，不要求每个案例组机械配齐；
- 同一案例组必须整体进入同一个训练集、验证集或测试集，不得拆分。

审批稿在本步写入：

```markdown
# Cxxxx-Tnn｜案例名称

## 关联信息
- case_id：Cxxxx-Tnn
- real_case_id：RCxxxx
- source_id：SRCxxxx

## 题面与数据性质
- decision_point：
- text：
- construction_type：
- 选择理由：
- content_verification_status：pending
```

同一个 `real_case_id` 可以产生多个 `case_id`，但每个不同题面必须单独编号。AI不能自行批准草稿；人核对事实来源、时间边界、题面自然度和构造类型后，将 `content_verification_status` 从 `pending` 改为 `approved`。只有审批通过后才能进入第8步；此后如果修改 `text`，状态必须退回 `pending` 并重新审批。

**产物：** 一个唯一 `case_id`，以及判断时点、题面、构造类型、选择理由和审批状态。

**检查：** `case_id` 是否唯一并正确关联来源；题面是否自然且包含足够事实；是否存在答案泄漏或事后信息；每个关键事实能否找到依据；构造类型是否与实际题面一致；人工是否已标记为 `approved`。

**退回：** 删除全部关键事实导致无法判断；加入原文没有的收款账户、支付渠道、设备、IP、历史交易或银行风险名单，却仍标为 `public_case_rewritten`；AI直接批准自己的草稿；多个不同题面重复使用同一个 `case_id`。

### 第8步：标注答案、类型和证据（AI执行）

**输入：** 最终 `text` 和经过核验的来源事实。

**操作：** 填写 `label`、`fraud_type` 和 `evidence`。

AI把结果追加到同一个 `cases/Cxxxx-Tnn.md` 的“参考答案”部分，不创建第二份案例文件：

```markdown
## 参考答案
- label：
- fraud_type：
- evidence：
```

`label` 只允许：

- `fraud`：当前事实足以支持欺诈判断；
- `normal`：当前行为有合理、可核验的正常解释；
- `insufficient`：现有信息不足以在欺诈和正常之间作出可靠判断。

已知母案例最终发生诈骗或正常结束，不能代替题面证据。只出现“投资”“客服”“贷款”“警察”“恋爱”等主题词，或只知道对方要求“操作一下”，都不足以直接标为 `fraud`；正常题面也必须明确给出能够独立核验的闭环。

`fraud_type` 使用团队统一枚举：

- `fake_investment`
- `task_rebate`
- `impersonated_customer_service`
- `impersonated_law_enforcement`
- `impersonated_acquaintance_or_leader`
- `loan_or_credit`
- `phishing_or_account_takeover`
- `ticket_game_virtual_goods`
- `romance_or_trust_fraud`
- `ai_impersonation`
- `other_fraud`
- `normal`
- `insufficient`

`label=normal` 时，`fraud_type` 必须是 `normal`；`label=insufficient` 时必须是 `insufficient`。遇到无法归类的欺诈先使用 `other_fraud`，不得由个人临时创造新类别。

`evidence` 只记录 `text` 中可以直接定位的决定性短语。第一阶段要求尽量使用题面原句，至少一项；不能把来源中的事后结论塞进证据。

**产物：** 一组相互一致的监督答案。

**检查：** 标签是否由多项事实共同支持；每条证据能否在题面中找到；类型是否符合枚举和标签一致性规则。

**退回：** 只凭“导师”“客服”“投资”等单词判为欺诈；证据来自法院判决但没有出现在题面；正常案例的类型仍写成某类欺诈。

### 第9步：完善 Markdown 审批稿（AI执行）

**输入：** 来源记录、事实边界、题面和标注结果。

**操作：** 在第7步创建的 `cases/Cxxxx-Tnn.md` 中补齐并整理以下内容，不再另建审批文件：

```markdown
# Cxxxx-Tnn｜案例名称

## 来源与关联
- case_id：
- source_id：
- real_case_id：
- 标题：
- 发布机构：
- 内容来源：
- 原始链接：
- 发布日期：
- 访问日期：
- 来源等级：
- 必要原文摘录：

## 事实边界
- decision_point：
- 可进入题面的事前事实：
- 只能用于核对答案的事后事实：
- 来源没有提供的信息：

## 题面与数据性质
- text：
- construction_type：
- 选择理由：
- content_verification_status：approved

## 参考答案
- label：
- fraud_type：
- evidence：

## 人工审批
- review_status：pending
- reviewer：null
- reviewed_at：null
- review_note：[]
```

AI 生成审批稿时统一使用以上默认值。人工只修改有误内容并作出审批决定；没有审批意见时，`review_note` 保持 `[]`。

必要原文摘录只保留支持当前案例的最小范围，并遵守版权、隐私和脱敏要求。

**产物：** 一份人能够核对全部关键判断的审批稿。

**检查：** 审批人是否无需重新搜索网页，就能看出题面事实来自哪里、删除了什么、增加了什么。

**退回：** 只有评分没有事实依据；只有改写文本没有原文摘录；结构化 JSON 中出现审批稿没有记录的新事实。

### 第10步：生成 JSON 结构化记录（AI执行）

**输入：** 已完成的 Markdown 审批稿。

**本阶段目标：** 第一阶段只训练和评测“根据当前题面判断是否欺诈”。JSON 不负责追问、风险分级和干预决策。

#### 10.1 为什么要生成 JSON

Markdown 用于人工阅读和审批，JSON 用于程序校验、训练和评测。第10步不再创造事实或修改答案，只把第9步已经确认的内容转换成固定格式。

每个 JSON 只表达一个判断场景：一个时间点、一段题面、一个主标签和一组证据。即使来自同一个真实案件，不同时间点或不同信息量的题面也必须使用不同的 `case_id`。

#### 10.2 第一阶段字段

在 `structured/Cxxxx-Tnn.json` 生成第一阶段记录，固定使用以下十个字段：

```json
{
  "case_id": "C0001-F01",
  "real_case_id": "RC0001",
  "text": "我在社交平台认识了一名自称投资导师的人。对方发来群内盈利截图，并让我在一个尚未独立核验的平台充值。",
  "label": "fraud",
  "fraud_type": "fake_investment",
  "evidence": [
    "自称投资导师",
    "发来群内盈利截图",
    "让我在一个尚未独立核验的平台充值"
  ],
  "source_id": "SRC0002",
  "construction_type": "public_case_rewritten",
  "review_status": "pending",
  "schema_version": "1.0"
}
```

十个字段分成四组：

**追溯字段：**

- `case_id`：结构化案例唯一编号；
- `real_case_id`：这条数据所属的真实案件编号。本 SOP 生成的忠实改写、改变组和正常对照都必须保留母案例的 `real_case_id`；
- `source_id`：来源登记表中的稳定编号；
- `construction_type`：忠实改写、受控合成或正常对照。

**模型输入字段：**

- `text`：模型在判断时唯一允许看到的题面；

**参考答案字段：**

- `label`：欺诈、正常或信息不足；
- `fraud_type`：统一的主类型；
- `evidence`：题面中支持答案的决定性事实。

**治理字段：**

- `review_status`：人工审批状态；
- `schema_version`：当前结构版本，第一阶段固定为 `1.0`。

`review_status` 只允许 `draft`、`pending`、`approved`、`disputed` 和 `rejected`。生成 JSON 后使用 `pending`；只有审批人可以改成 `approved`。

#### 10.3 颗粒度标准

- `text`：只描述一次判断所需的事前信息，不混入案件经过、参考答案、追问或处置建议；
- `label`：每条数据只能有一个主标签，取值为 `fraud`、`normal` 或 `insufficient`；
- `fraud_type`：只保留一个主类型。第一阶段不增加次级类型和多标签；
- `evidence`：通常保留2至5条决定性短语，必须能在 `text` 中直接定位。欺诈案例记录风险行为组合，正常案例记录可核验的正常闭环，信息不足案例记录造成歧义的已知事实；
- `case_id`：不同题面使用不同编号，不能只修改原 JSON 覆盖另一种题面；
- 缺失信息保持缺失，不能用空字符串、猜测值或默认值伪装成已知事实。

不要加入 `confidence`、`risk_level`、`missing_information`、`recommended_questions`、`recommended_intervention`、`conversation`、`difficulty`、`split` 等字段。它们不服务第一阶段的欺诈判断，会增加审批成本并混淆训练目标。

#### 10.4 生成顺序

1. 从审批稿复制 `case_id`、`source_id`、`real_case_id` 和 `construction_type`；
2. 从第7步复制最终 `text`；
3. 从第8步复制 `label`、`fraud_type` 和 `evidence`；
4. 将 `review_status` 设为 `pending`，将 `schema_version` 设为 `1.0`；
5. 按第11步自动检查，不得在生成过程中补写审批稿中不存在的内容。

#### 10.5 后续阶段怎么扩展

当前只实施第一阶段。以后确实开始训练其他能力时再升级 Schema：

- 第二阶段训练追问能力，再增加缺失信息和建议问题；
- 第三阶段训练处置能力，再增加风险等级和干预建议。

后续扩展时保留现有字段及其含义，通过新的 `schema_version` 增加字段并编写迁移脚本。一个案例只保留一份权威结构化记录，不长期人工维护两套内容重复但字段不同的 JSON。

**产物：** 可以被程序解析的单案例 JSON 文件。

**检查：** 十个字段是否齐全且没有阶段外字段；字段值是否与 Markdown 完全一致；`evidence` 是否能在 `text` 中定位；`source_id` 和 `real_case_id` 是否存在并相互对应；JSON 是否可解析。

**退回：** JSON 混入追问、干预等后续字段；一条记录包含多个判断时间点；自动生成程序带入空字符串、猜测内容或旧字段；JSON 与审批稿不同；自动检查通过后直接写成 `approved`。

### 第11步：自动检查和人工审批（人机协作，人工终审）

**输入：** Markdown 审批稿和 JSON 文件。

**操作：** 先自动检查，再人工审批。

自动检查至少包括：

- JSON 语法正确；
- 十个必填字段完整；
- `case_id` 唯一且文件名一致；
- `source_id` 能在来源登记表中找到；
- `real_case_id` 非空，能在真实案件登记表中找到，并且对应同一个 `source_id`；
- 枚举值合法，标签与类型一致；
- `evidence` 非空，并能在 `text` 中直接定位；
- 文本不存在明显未脱敏的手机号、银行卡号、身份证号等信息；
- 重复文本和高度相似文本被标出供人工复核。

以后划分训练集、验证集和测试集时，必须按 `real_case_id` 整组划分，不能按 `case_id` 随机拆分。

人工审批必须检查：

- 来源是否真的支持题面关键事实；
- 构造类型是否诚实；
- 是否存在事后答案泄漏；
- 标签和类型是否合理；
- 证据是否足以支持答案；
- 是否只靠固定关键词判断；
- 是否虚构银行可以获取的信息。

审批结论：

- `approved`：来源、题面、标签和证据均通过；
- `disputed`：存在需要团队共同判断的问题，暂不使用；
- `rejected`：存在明确错误，退回修改或废弃。

只有自动检查通过、人工状态为 `approved`、审批记录已写回 Markdown 且修改已由 Git 留痕，才算正式完成；其他状态都只能算草稿。

**产物：** 有明确人工状态的正式数据或退回记录。

**检查：** 审批人、时间和理由是否写回 Markdown；JSON 状态是否与审批稿一致。

**退回：** 只有AI评分或自动脚本结论，没有人工审批；审批理由为空；修改 JSON 后没有重新检查。
